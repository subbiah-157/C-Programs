#include <stdio.h>

int main()
{
    int i,j=0,rows;
    printf("Enter a rows:");
    scanf("%d", &rows);
    for(i=0; i<=rows; i++)
    {
       for(j=rows; j>i; j--)
       {
           printf("* ");
       }
       printf("\n");
    }
    return 0;
}
